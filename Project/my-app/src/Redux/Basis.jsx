/* import redrowPage from '../redrow';

let basis = {
    mainPage: {
        newTextSobitie: 'Введите текст...'
    }
}

export let addText = () => {
    let newSobitie = {
        id: 1,
        text: basis.mainPage.newTextSobitie
    };
    basis.mainPage.newTextSobitie.push(newSobitie);
    basis.mainPage.newTextSobitie = '';
    redrowPage(basis);
};

export let updateText = (newSobitie) => {
    basis.mainPage.newTextSobitie = newSobitie;
    redrowPage(basis);
}

export default basis;  */